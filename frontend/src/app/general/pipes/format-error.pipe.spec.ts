import { FormatErrorPipe } from './format-error.pipe';

describe('FormatErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
